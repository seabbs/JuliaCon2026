---
title: "Estimating epidemiological delay distributions"
subtitle: "From R/Stan to Julia"
description: >-
  CensoredDistributions.jl, and an honest comparison with the R and Stan
  implementation it grew out of.
page-layout: full
---

::: {.session-meta}
JuliaCon 2026 · Friday 14 August 2026, 14:45–15:00 (Europe/Berlin) ·
Muschel — N3 · Sam Abbott
:::

[Slides](slides.qmd){.btn .btn-primary}
[CensoredDistributions.jl][cd-repo]{.btn .btn-outline-primary}
[Documentation][cd-docs]{.btn .btn-outline-primary}
[Session on pretalx][pretalx]{.btn .btn-outline-primary}

:::: {.talk-body}
::: {.talk-main}

## Abstract

::: {.abstract}
Delay distributions describe the time between epidemiological events, such
as infection to symptom onset or symptom onset to hospitalisation.
Estimating these distributions from outbreak data is difficult because both
the primary event (e.g. infection) and the secondary event (e.g. symptom
onset) are usually only known to have occurred within a time window, such as
a day.
Real-time outbreak data is also often right-truncated as longer delays have
not yet been observed.
Ignoring double interval censoring and truncation biases parameter estimates
which are then used for forecasting and transmission modelling.

Adjusting distributions for primary event censoring addresses this by
integrating the delay CDF over the primary event window, weighted by the
density of when, within the window, the event occurred.
This can then be combined with truncation and secondary interval-censoring
adjustments to produce a double-interval-censored and
right-truncation-adjusted distribution.

In this talk, we present CensoredDistributions.jl, which implements these
adjustments as `primary_censored`, `interval_censored`, and
`double_interval_censored`, composable Distributions.jl wrappers.
Multiple dispatch selects closed-form CDFs for delay and primary event
distribution pairs where these are available, and falls back to numerical
integration otherwise.
We demo the package standalone and with Turing.jl for parameter estimation.

We then compare to primarycensored, our equivalent R package, which also
ships a duplicate set of Stan functions so users can fit models in either
language.
Maintaining two parallel implementations required reimplementing
distribution functions in Stan, building tooling to vendor Stan code into
downstream projects, and replacing types with integer distribution
identifiers.
Stan's integral solver was also unstable for this problem, so we had to
recast it as an ODE.
Julia's multiple dispatch and ecosystem composability eliminates all of
this.

We then summarise our plans to build a composed Julia version of our epidist
R package, using CensoredDistributions.jl as a foundation with Turing.jl
submodels for partially pooled and flexible delay estimation.
:::

One correction since submission.
Multiple dispatch removes the integer distribution tables and ecosystem
composability removes the vendoring tool, but neither removes the numerical
problem underneath.
CensoredDistributions.jl dropped adaptive quadrature from the default path
too, for the same reason Stan did.
Stan swapped the integral for an ODE solver.
Julia's default is now a fixed 64-node rule, with the adaptive solver still
there for callers who ask for it.
Both changes were forced by the differentiator, not by the mathematics.

:::

::: {.talk-side}

::: {.talk-qr}
![](../figures/qr-delays.png){fig-alt="QR code linking to this page"}

The QR code on the poster and on the closing slide points here.
:::

## Resources

### The R and Stan side this grew out of

- [primarycensored][pc].
  The R package, over 11,000 CRAN downloads all-time, and this talk's
  starting point.
- [epidist][epidist].
  Fits delay distributions on top of primarycensored.
- [primarycensored PR #64][pc-pr64].
  The ODE recast forced by Stan's integral solver going unstable
  mid-sampling.
- [primarycensored PR #277][pc-pr277].
  The dist_id bug, where an integer meant Normal in Stan and Weibull in R.
- [primarycensored on Zenodo][pc-zenodo].
  The concept DOI, which always resolves to the latest release.

### Julia

- [ConvolvedDistributions.jl][conv-repo].
  Sums, differences, products, and ratios of delay distributions, the next
  layer up.
- [CensoredDistributions.jl PR #250][cd-pr250].
  The switch to a fixed rule, and what it cost.

### Related talks

The other two JuliaCon talks are in the same room on the same day.

- [Building a composable Julia ecosystem](../roadmap/index.qmd).
  Where CensoredDistributions.jl sits in the wider ecosystem.
  14:30–14:45, straight before this one.
- [Composable probabilistic models](../composable/index.qmd).
  The case for building models out of parts like these.
  16:45–17:00.
- [How to serial interval?][hsi].
  More on censoring, delays, and why the serial interval is itself a
  composition.

:::
::::

[cd-repo]: https://github.com/EpiAware/CensoredDistributions.jl
[cd-docs]: https://censoreddistributions.epiaware.org/stable/
[pretalx]: https://pretalx.com/juliacon-2026/talk/ZUMSFD/
[pc]: https://primarycensored.epinowcast.org/
[epidist]: https://epidist.epinowcast.org/
[pc-pr64]: https://github.com/epinowcast/primarycensored/pull/64
[pc-pr277]: https://github.com/epinowcast/primarycensored/pull/277
[pc-zenodo]: https://doi.org/10.5281/zenodo.13632838
[conv-repo]: https://github.com/EpiAware/ConvolvedDistributions.jl
[cd-pr250]: https://github.com/EpiAware/CensoredDistributions.jl/pull/250
[hsi]: https://samabbott.co.uk/how-to-serial-interval
