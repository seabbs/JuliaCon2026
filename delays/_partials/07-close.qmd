## In my view the Julia version is clearly better

- Any distribution, one implementation
- It is also the one nobody uses
- I would still point an R user at `primarycensored` today
- Next is a composed Julia `epidist`, with Turing submodels doing what
  the `brms` formula does now

::: {.callout-important}
So how do I get people to shift?
:::
