## Documentation

:::: {.columns}
::: {.column width="52%"}
- Julia's default is a docstring if you feel like it
- `DocStringExtensions` gets us close to R's standard. I would like more
- The README's Julia blocks run at docs build, so a broken example fails
  the build
- `SciML` puts something interesting on the front page and keeps the
  tutorials short. We copy that
- The banned word list is one of the checks. A README that calls itself
  comprehensive does not ship
:::
::: {.column width="48%"}
```julia
const BANNED_README_WORDS = [
    "comprehensive", "cornerstone",
    "facilitate", "foster",
    "framework", "harness",
    "landscape", "leverage",
    "novel", "pivotal",
    "practitioner", "robust",
    "streamline", "synergy",
    # ... 20 in total
]
```

::: {.attribution}
`BANNED_README_WORDS` in `src/quality.jl`, and the sixteen written
standards in the `EpiAwarePackageTools.jl` documentation, both read
2026-08-12 ·
[DocStringExtensions.jl](https://github.com/JuliaDocs/DocStringExtensions.jl).
:::
:::
::::
